Command to create the bulk episodes file is "dir -n > _list.txt" 

If you're having trouble adding dependencies with composer. Getting "Script symfony-cmd handling the auto-scripts event returned with error code 1" 
then rrun the same cmd with --no-scripts. 