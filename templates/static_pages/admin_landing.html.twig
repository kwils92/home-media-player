{% extends 'base.html.twig' %}

{% block title %}Gonna watch something? Nice.{% endblock %}

{% block body %}

    {% include 'modules/nav.html.twig' %}

    <ul class="c-admin__links">
        <li class="">
            <a href="{{ path('new_batch') }}" class="m-remove-underline m-blink-shadow-colour__text">Upload a New Season (batch)</a>
        </li>
        <li>
            <a href="{{ path('new_movie_batch') }}" class="m-remove-underline m-blink-shadow-colour__text">Upload Movies (batch)</a>
        </li>

        <li>
            <a href="{{ path('movies_new') }}" class="m-remove-underline m-blink-shadow-colour__text">Upload a Movie (single)</a>
        </li>

        <li>
            <a href="{{ path('shows_new') }}" class="m-remove-underline m-blink-shadow-colour__text">Upload a Show (single)</a>
        </li>
    </ul>


{% endblock %}