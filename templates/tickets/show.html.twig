{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block body %}
    {% include 'modules/nav.html.twig' %}
    
    <h1>Tickets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idtickets</th>
                <td>{{ ticket.idtickets }}</td>
            </tr>
            <tr>
                <th>Request</th>
                <td>{{ ticket.request }}</td>
            </tr>
            <tr>
                <th>Outcome</th>
                <td>{{ ticket.outcome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tickets_index') }}">back to list</a>

    <a href="{{ path('tickets_edit', {'idtickets': ticket.idtickets}) }}">edit</a>

    {{ include('tickets/_delete_form.html.twig') }}
{% endblock %}
